/* IAIS UAE news page */
let filteredArticles=[...ARTICLES];
const safe=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function renderFeaturedArticle(){const c=document.getElementById('featuredArticle');if(!c)return;const a=filteredArticles[0];c.innerHTML=a?`<img src="${safe(a.img)}" alt="${safe(a.title)}" loading="lazy"><div class="nf-body"><span class="news-cat">${safe(a.cat)}</span><h3>${safe(a.title)}</h3><p>${safe(a.excerpt)}</p><div class="news-meta"><span>📅 ${safe(a.date)}</span>${a.author?`<span>👤 ${safe(a.author)}</span>`:''}${a.read?`<span>⏱ ${safe(a.read)}</span>`:''}</div></div>`:'<div class="news-empty"><h3>No featured article available.</h3></div>'}
function renderNewsGrid(list){const c=document.getElementById('newsGrid');if(!c)return;const news=list.slice(1);c.innerHTML=news.length?news.map(a=>`<article class="news-card"><img src="${safe(a.img)}" alt="${safe(a.title)}" loading="lazy"><div class="nc-body"><span class="news-cat">${safe(a.cat)}</span><h4>${safe(a.title)}</h4><p>${safe(a.excerpt)}</p><div class="news-meta"><span>📅 ${safe(a.date)}</span>${a.read?`<span>⏱ ${safe(a.read)}</span>`:''}</div></div></article>`).join(''):'<div class="news-empty"><h3>No articles found</h3><p>Try another search term or category.</p></div>'}
function renderAll(){renderFeaturedArticle();renderNewsGrid(filteredArticles)}
function initializeCategories(){document.querySelectorAll('.news-categories button').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.news-categories button').forEach(x=>x.classList.remove('active'));b.classList.add('active');const cat=b.textContent.trim();filteredArticles=cat==='All'?[...ARTICLES]:ARTICLES.filter(a=>a.cat.toLowerCase()===cat.toLowerCase());const q=document.getElementById('newsSearch');if(q)q.value='';renderAll()}))}
function initializeSearch(){const q=document.getElementById('newsSearch');if(!q)return;q.addEventListener('input',()=>{const t=q.value.trim().toLowerCase();filteredArticles=ARTICLES.filter(a=>[a.title,a.excerpt,a.cat].some(v=>String(v).toLowerCase().includes(t)));renderAll()});document.querySelector('.search-wrapper button')?.addEventListener('click',()=>q.dispatchEvent(new Event('input')))}
document.addEventListener('DOMContentLoaded',()=>{renderAll();initializeCategories();initializeSearch();});
